# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'NoTouch' do
  use_frameworks!
  pod 'Firebase/Crashlytics'

end
  
target 'NoTouchTests' do
  pod 'Firebase/Crashlytics'

end
